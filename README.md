# 🛒 MCP SQLite Shop Demo

โปรเจกต์ตัวอย่างสำหรับการเรียนรู้ **Model Context Protocol (MCP)** โดยการสร้าง MCP Server ด้วย Python เพื่อเชื่อมต่อ Claude Desktop เข้ากับฐานข้อมูล SQLite (จำลองร้านค้าออนไลน์)

## สิ่งที่ต้องเตรียม (Prerequisites)

- **Claude Desktop App**: ดาวน์โหลดและติดตั้งจาก [claude.ai/download](https://claude.ai/download)
- **VS Code**: สำหรับแก้ไขโค้ดและตั้งค่า
- **Terminal**: PowerShell (Windows) หรือ Terminal (Mac/Linux)

---

## ขั้นตอนที่ 1: ติดตั้ง uv (Python Package Manager)

เราจะใช้ `uv` ในการจัดการ Environment และรันโปรแกรม เพราะรวดเร็วและจัดการง่าย

**สำหรับ Windows (PowerShell):**
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**สำหรับ macOS / Linux:**
```bash
curl -lsSf https://astral.sh/uv/install.sh | sh
```

> **เช็คว่าติดตั้งสำเร็จหรือไม่**: ปิด Terminal แล้วเปิดใหม่ จากนั้นพิมพ์คำสั่ง:
> ```bash
> uv --version
> ```
> ถ้าขึ้นตัวเลขเวอร์ชัน (เช่น `uv 0.5.x`) แปลว่าพร้อมลุย!

---

## ขั้นตอนที่ 2: ตั้งค่าโปรเจกต์ (Project Setup)

1. **สร้างโฟลเดอร์สำหรับโปรเจกต์** และเปิด Terminal ที่โฟลเดอร์นั้น
2. **เริ่มสร้าง Environment**:
   ```bash
   uv init
   ```
3. **ติดตั้ง Library ที่จำเป็น (MCP SDK)**:
   ```bash
   uv add "mcp[cli]"
   ```

---

## ขั้นตอนที่ 3: เตรียมฐานข้อมูลและโค้ด

ตรวจสอบว่าในโฟลเดอร์ของคุณมีไฟล์ครบทั้ง 2 ไฟล์นี้:

- `create_db.py`: สคริปต์สำหรับสร้างข้อมูลจำลอง (Users, Products, Orders)
- `main.py`: โค้ด MCP Server หลัก

**สร้างฐานข้อมูล (Mock Data)**:
รันคำสั่งนี้เพื่อสร้างไฟล์ `shop.db`:
```bash
uv run create_db.py
```
คุณจะเห็นไฟล์ `shop.db` ปรากฏขึ้นมาในโฟลเดอร์

---

## ขั้นตอนที่ 4: เชื่อมต่อกับ Claude Desktop

นี่คือขั้นตอนที่สำคัญที่สุด! เราต้องบอกให้ Claude รู้จัก Server ของเรา

1. เปิด **Claude Desktop**
2. ไปที่ **File > Settings > Developer**
3. กดปุ่ม **Edit Config** (ไฟล์ `claude_desktop_config.json` จะเด้งขึ้นมา)
4. ก๊อปปี้โค้ดด้านล่างไปวาง (ทับของเดิม หรือเพิ่มใน `mcpServers`):

> [!IMPORTANT]
> **⚠️ สำคัญ**: เปลี่ยน `USERNAME` และ `Path` ให้ตรงกับเครื่องของคุณ!
> (แนะนำให้ใช้ VS Code คลิกขวาที่ไฟล์ `main.py` -> **Copy Path** แล้วเปลี่ยน `\` เป็น `\\`)

```json
{
  "mcpServers": {
    "shop-db": {
      "command": "C:\\Users\\USERNAME\\.local\\bin\\uv.exe",
      "args": [
        "run",
        "--quiet",
        "--directory",
        "C:\\Users\\USERNAME\\path\\to\\your\\project",
        "main.py"
      ]
    }
  }
}
```

**คำอธิบาย Config:**
- `command`: ที่อยู่ของไฟล์ `uv.exe` (เช็คได้โดยพิมพ์ `(Get-Command uv).Source` ใน PowerShell)
- `--directory`: สั่งให้ย้ายไปทำงานในโฟลเดอร์โปรเจกต์ (เพื่อกันปัญหาหาไฟล์ `shop.db` ไม่เจอ)

---

## ขั้นตอนที่ 5: เริ่มใช้งาน

1. **Save ไฟล์ Config**
2. **Restart โปรแกรม Claude Desktop** (ปิดแล้วเปิดใหม่)
3. สังเกตสัญลักษณ์ 🔌 (รูปปลั๊กไฟ) ที่มุมขวาบน ถ้าขึ้นแสดงว่าเชื่อมต่อสำเร็จ!

**ตัวอย่างคำถามที่ลองถาม Claude ได้:**
- "ช่วยดู Database schema ให้หน่อยว่ามีตารางอะไรบ้าง?"
- "มีสินค้าอะไรบ้างที่ราคาเกิน 10,000 บาท?"
- "ใครคือลูกค้าที่มียอดซื้อรวมสูงสุด 5 อันดับแรก?"
- "เช็คสต็อกสินค้าหมวด Electronics ให้หน่อย"

---

## การแก้ปัญหาเบื้องต้น (Troubleshooting)

- **Error: Server disconnected หรือ Executable not found**
  - เช็ค Path ในไฟล์ Config ให้ดี ต้องใช้ `\\` (Backslash คู่) เสมอใน Windows
  - เช็คว่าที่อยู่ของ `uv.exe` ถูกต้อง

- **Error: ModuleNotFoundError: No module named 'mcp'**
  - ตรวจสอบว่าใส่บรรทัด `--directory` ใน Config แล้วหรือยัง
  - ตรวจสอบว่าในโฟลเดอร์โปรเจกต์มีไฟล์ `pyproject.toml` อยู่
